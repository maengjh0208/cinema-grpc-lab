fastapi==0.137.1
uvicorn==0.49.0             # FastAPI 실행 서버
sqlalchemy==2.0.51          # ORM
psycopg2-binary==2.9.12     # PostgreSQL 드라이버 (-binary는 C 컴파일 없이 설치되는 버전으로 학습용으로 적합. 프로덕션에서는 psycopg2 쓰는걸 권장)
pyjwt==2.13.0               # JWT 검증
grpcio==1.81.1              # gRPC 런타임
grpcio-tools==1.81.1        # protoc 코드 생성 도구